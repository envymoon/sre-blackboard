# host-alert SKILL (spec Sec.13.4): node health and peer comparison. PROBLEM_HOST_CONFIRMED needs both runtime-anomaly and service-impact evidence plus >= 2 healthy peer instances.
# Script chain: load_host_snapshot -> select_peer_hosts -> correlate_service_impact
